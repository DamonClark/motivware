# frozen_string_literal: true

RSpec.describe Motivware do
  it "has a version number" do
    expect(Motivware::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
